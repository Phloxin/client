{
  "name": "audio-capture",
  "version": "0.1.0",
  "private": true,
  "description": "Native per-application audio capture (WASAPI process loopback / PipeWire) for screenshare audio",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "audio-capture"
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  }
}
